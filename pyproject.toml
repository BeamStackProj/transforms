[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "beamstack_transforms"
version = "0.1.2"
requires-python = ">=3.10"
dependencies = ["apache_beam"]
